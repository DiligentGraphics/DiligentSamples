Texture2D g_Tex;
SamplerState g_Tex_sampler;

struct PSInput 
{ 
    float4 Pos : SV_POSITION; 
    float TexCoord : TEX_COORD;
};

float4 main(PSInput ps_in) : SV_TARGET
{
    return g_Tex.Sample(g_Tex_sampler, float2(ps_in.TexCoord, 0.5));
}
